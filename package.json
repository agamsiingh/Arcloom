{
  "name": "arcloom",
  "version": "1.0.0",
  "description": "ARCLOOM - a minimalist arrow-weaving puzzle game (offline PWA)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/bake.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
